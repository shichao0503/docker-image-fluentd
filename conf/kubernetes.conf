# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/kubernetes.conf.erb

<match fluent.**>
  @type null
</match>

<source>
  @type tail
  @id in_tail_container_logs_nginx-ingress-ds
  path /var/log/containers/nginx-ingress-ds*.log
  pos_file /var/log/fluentd-containers-nginx-ingress-ds.log.pos
  tag kubernetes.nginx-ingress-ds
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-backend-service
  path /var/log/containers/credit-backend*beichen_service*.log
  pos_file /var/log/fluentd-containers-credit-backend-service.log.pos
  tag kubernetes.credit-backend-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-backend-phpfpm
  path /var/log/containers/credit-backend*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-credit-backend-phpfpm.log.pos
  tag kubernetes.credit-backend-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-backend-import
  path /var/log/containers/credit-backend-import*beichen_service*.log
  pos_file /var/log/fluentd-containers-credit-backend-import.log.pos
  tag kubernetes.credit-backend-import
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-portal-service
  path /var/log/containers/credit-portal*beichen_service*.log
  pos_file /var/log/fluentd-containers-credit-portal-service.log.pos
  tag kubernetes.credit-portal-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-portal-phpfpm
  path /var/log/containers/credit-portal*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-credit-portal-phpfpm.log.pos
  tag kubernetes.credit-portal-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-oa-service
  path /var/log/containers/credit-oa*beichen_service*.log
  pos_file /var/log/fluentd-containers-credit-oa-service.log.pos
  tag kubernetes.credit-oa-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-oa-phpfpm
  path /var/log/containers/credit-oa*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-credit-oa-phpfpm.log.pos
  tag kubernetes.credit-oa-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-share-service
  path /var/log/containers/credit-share*beichen_service*.log
  pos_file /var/log/fluentd-containers-credit-share-service.log.pos
  tag kubernetes.credit-share-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credit-share-phpfpm
  path /var/log/containers/credit-share*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-credit-share-phpfpm.log.pos
  tag kubernetes.credit-share-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<filter kubernetes.**>
  @type kubernetes_metadata
  @id filter_kube_metadata
  kubernetes_url "#{ENV['FLUENT_FILTER_KUBERNETES_URL'] || 'https://' + ENV.fetch('KUBERNETES_SERVICE_HOST') + ':' + ENV.fetch('KUBERNETES_SERVICE_PORT') + '/api'}"
  verify_ssl "#{ENV['KUBERNETES_VERIFY_SSL'] || true}"
  ca_file "#{ENV['KUBERNETES_CA_FILE']}"
  @type parser
  format json
  key_name log
  reserve_data true
</filter>

