# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/kubernetes.conf.erb

<match fluent.**>
  @type null
</match>

<source>
  @type tail
  @id in_tail_container_logs_nginx-ingress-ds
  path /var/log/containers/nginx-ingress-ds*.log
  pos_file /var/log/fluentd-containers-nginx-ingress-ds.log.pos
  tag kubernetes.nginx-ingress-ds
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-backend-service
  path /var/log/containers/credityb-backend*yibin_service*.log
  pos_file /var/log/fluentd-containers-credityb-backend-service.log.pos
  tag kubernetes.credityb-backend-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-backend-phpfpm
  path /var/log/containers/credityb-backend*yibin_phpfpm*.log
  pos_file /var/log/fluentd-containers-credityb-backend-phpfpm.log.pos
  tag kubernetes.credityb-backend-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-backend-import
  path /var/log/containers/credityb-backend-import*yibin_service*.log
  pos_file /var/log/fluentd-containers-credityb-backend-import.log.pos
  tag kubernetes.credityb-backend-import
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-portal-service
  path /var/log/containers/credityb-portal*yibin_service*.log
  pos_file /var/log/fluentd-containers-credityb-portal-service.log.pos
  tag kubernetes.credityb-portal-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-portal-phpfpm
  path /var/log/containers/credityb-portal*yibin_phpfpm*.log
  pos_file /var/log/fluentd-containers-credityb-portal-phpfpm.log.pos
  tag kubernetes.credityb-portal-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-oa-service
  path /var/log/containers/credityb-oa*yibin_service*.log
  pos_file /var/log/fluentd-containers-credityb-oa-service.log.pos
  tag kubernetes.credityb-oa-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-oa-phpfpm
  path /var/log/containers/credityb-oa*yibin_phpfpm*.log
  pos_file /var/log/fluentd-containers-credityb-oa-phpfpm.log.pos
  tag kubernetes.credityb-oa-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-share-service
  path /var/log/containers/credityb-share*yibin_service*.log
  pos_file /var/log/fluentd-containers-credityb-share-service.log.pos
  tag kubernetes.credityb-share-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_credityb-share-phpfpm
  path /var/log/containers/credityb-share*yibin_phpfpm*.log
  pos_file /var/log/fluentd-containers-credityb-share-phpfpm.log.pos
  tag kubernetes.credityb-share-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<filter kubernetes.**>
  @type kubernetes_metadata
  @id filter_kube_metadata
  kubernetes_url "#{ENV['FLUENT_FILTER_KUBERNETES_URL'] || 'https://' + ENV.fetch('KUBERNETES_SERVICE_HOST') + ':' + ENV.fetch('KUBERNETES_SERVICE_PORT') + '/api'}"
  verify_ssl "#{ENV['KUBERNETES_VERIFY_SSL'] || true}"
  ca_file "#{ENV['KUBERNETES_CA_FILE']}"
  @type parser
  format json
  key_name log
  reserve_data true
</filter>

