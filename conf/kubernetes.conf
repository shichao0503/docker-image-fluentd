# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/kubernetes.conf.erb

<match fluent.**>
  @type null
</match>

<source>
  @type tail
  @id in_tail_container_logs_nginx-ingress-ds
  path /var/log/containers/nginx-ingress-ds*.log
  pos_file /var/log/fluentd-containers-nginx-ingress-ds.log.pos
  tag kubernetes.nginx-ingress-ds
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-backend-service
  path /var/log/containers/creditbc-backend*beichen_service*.log
  pos_file /var/log/fluentd-containers-creditbc-backend-service.log.pos
  tag kubernetes.creditbc-backend-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-backend-phpfpm
  path /var/log/containers/creditbc-backend*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-creditbc-backend-phpfpm.log.pos
  tag kubernetes.creditbc-backend-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-backend-import
  path /var/log/containers/creditbc-backend-import*beichen_service*.log
  pos_file /var/log/fluentd-containers-creditbc-backend-import.log.pos
  tag kubernetes.creditbc-backend-import
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-portal-service
  path /var/log/containers/creditbc-portal*beichen_service*.log
  pos_file /var/log/fluentd-containers-creditbc-portal-service.log.pos
  tag kubernetes.creditbc-portal-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-portal-phpfpm
  path /var/log/containers/creditbc-portal*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-creditbc-portal-phpfpm.log.pos
  tag kubernetes.creditbc-portal-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-oa-service
  path /var/log/containers/creditbc-oa*beichen_service*.log
  pos_file /var/log/fluentd-containers-creditbc-oa-service.log.pos
  tag kubernetes.creditbc-oa-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-oa-phpfpm
  path /var/log/containers/creditbc-oa*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-creditbc-oa-phpfpm.log.pos
  tag kubernetes.creditbc-oa-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-share-service
  path /var/log/containers/creditbc-share*beichen_service*.log
  pos_file /var/log/fluentd-containers-creditbc-share-service.log.pos
  tag kubernetes.creditbc-share-service
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<source>
  @type tail
  @id in_tail_container_logs_creditbc-share-phpfpm
  path /var/log/containers/creditbc-share*beichen_phpfpm*.log
  pos_file /var/log/fluentd-containers-creditbc-share-phpfpm.log.pos
  tag kubernetes.creditbc-share-phpfpm
  read_from_head true
  <parse>
    @type "#{ENV['FLUENT_CONTAINER_TAIL_PARSER_TYPE'] || 'json'}"
    time_format %Y-%m-%dT%H:%M:%S.%NZ
  </parse>
</source>

<filter kubernetes.**>
  @type kubernetes_metadata
  @id filter_kube_metadata
  kubernetes_url "#{ENV['FLUENT_FILTER_KUBERNETES_URL'] || 'https://' + ENV.fetch('KUBERNETES_SERVICE_HOST') + ':' + ENV.fetch('KUBERNETES_SERVICE_PORT') + '/api'}"
  verify_ssl "#{ENV['KUBERNETES_VERIFY_SSL'] || true}"
  ca_file "#{ENV['KUBERNETES_CA_FILE']}"
  @type parser
  format json
  key_name log
  reserve_data true
</filter>

